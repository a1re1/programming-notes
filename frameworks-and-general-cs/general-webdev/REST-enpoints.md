# REST General Notes

## What is it?


## Routes

|Route|What is it used for?|
|---|---|
|GET()|read|
|POST()|create|
|DELETE()|remove|
|PUT()|update/replace|

## Testing
You can manually test services using chrome extension 'PostMan'
